# Copy this file to `.env` and fill in the values. Do NOT commit the filled `.env`.

# Google Generative Language API key (if you use Google LLM features)
GOOGLE_API_KEY=your_google_api_key_here

# Optional: a specific model resource string (if applicable to your setup)
# Examples: models/gemini-2.5-pro, models/gemini-2.5-flash
MODEL_NAME=models/gemini-2.5-pro

# Optional Google OAuth client credentials. If `client_secret.json` exists at project root,
# the app will try to load the client id/secret from that file. You can also set them here.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# OAuth redirect used by the app (default local dev value)
GOOGLE_OAUTH_REDIRECT=http://127.0.0.1:8000/api/calendar/oauth2callback

# API key used for browser/server calls to other Google APIs (if applicable)
# Keep this key limited to the required APIs or leave blank for local dev and use a server-side credential.
GOOGLE_API_KEY=

# Optional OpenAI API key if you configure OpenAI as a fallback or additional model provider
OPENAI_API_KEY=

# Flask secret used for session cookies (set a long random value for production)
SECRET_KEY=replace_with_a_random_secret

# Database (SQLite) path — default to the bundled local DB
DATABASE_URL=sqlite:///data/chronoken.db

# Server port
PORT=8000

# Notes:
# - After copying `.env.template` to `.env` set the keys and restart the server.
# - Never commit your `.env` file to source control. Keep secrets private.
