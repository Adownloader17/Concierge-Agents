# Copy this file to `.env` and fill in the values. Do NOT commit the filled `.env`.

GOOGLE_API_KEY=your_google_api_key_here
# Optional: choose a model from `list_models_lowlevel.py` output, e.g. models/gemini-2.5-flash
MODEL_NAME=models/gemini-2.5-flash
